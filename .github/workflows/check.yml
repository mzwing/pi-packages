name: Check Codebase

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: read
  statuses: write

jobs:
  check:
    name: Check Codebase
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version-file: .node-version
          check-latest: true

      - name: Install pnpm
        uses: pnpm/action-setup@v6
        with:
          cache: true

      - name: Install dependencies
        run: pnpm i --frozen-lockfile

      - name: Run format & lint & typecheck & unused file check
        run: |
          pnpm run format:check
          pnpm run lint
          pnpm run typecheck
          pnpm run knip

      - name: Run test
        run: pnpm run test
